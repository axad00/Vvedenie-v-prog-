#include <iostream>
#include <cstdlib>
#include<cmath>
использование пространства имен std;

int main() {
 int v1, v2, s, sk, t;
 cout < < "введите скорость певого авто" <;
 cin > > > v1;
 cout < < "введите скорость второго авто" <;
 cin > > > v2;
 cout < < "введите щель между ними" <;
 cin > > > s;
 cout < < "введите кол-во времени" <;
 cin > > > t;
 sk = (t * v1) + (t * v2) + s;
 cout < < "прощание мне авто через" < < t < < "часов будеть" <;
 возврат 0;
}
