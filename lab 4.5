#include <iostream>
#include <cstdlib>
#include<cmath>
использование пространства имен std;

int main() {
 поплавок x, a, b;
 cout < < "введите коэффициент pri X" <;
 cin > > > a;
 cout < < "введите коэффициент B" <;
 cin > > > b;
 x = (- b) / a;
 cout < < "X raven" <;
 возврат 0;
}
