#include <iostream>
#include <cstdlib>
#include<cmath>
использование пространства имен std;

int main() {
 поплавок a, x, y1, x1, y;
 cout < < "введите cenu konfet" <;
 cin > > > a;
 cout < < "вести ВЭС конфет" <;
 cin > > > x;
 y1 = a / x;
 cout < < "1 кг конфет стоит" < < y1 <;
 cout < < введите кол-во кг другого мешка конфет" <;
 cin >>> x1;
 y = y1 * x1;
 cout < < x1 < < "kg konfet stoit" < < y <;

 возврат 0;
}
