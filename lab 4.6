#include <iostream>
#include <cstdlib>
#include<cmath>
использование пространства имен std;

int main() {
 поплавок x, y, a1, b1, a2, b2, c1, c2, m;
 cout < < "введите первый коэффициент A" <;
 cin >>> a1;
 cout < < "введите первый коэффициент B" <;
 cin >>> b1;
 cout < < "введите первый коэффициент C" <;
 cin >>> c1;
 cout < < "введите второй коэффициент A" <;
 cin >>> a2;
 cout < < "введите второй коэффициент B" <;
 cin > > > b2;
 cout < < "введите второй коэффициент C" <;
 cin >>> c2;

 если (a1 > a2) {
 m = a1 / a2;
 y = ((c1 - (c2 * m)) / (b1 - (b2 * m)));

	}
 еще { 

 m = a2 / a1;
 y = ((c1 * m) - c2) / ((b1 * m) - b2);
	}
 x = (c1 - (b1 * y)) / a1;

 cout < < "X raven" < < x < < "Y raven" < < y <;

 возврат 0;
}
